# STI College CDO - Attendance System

## Used in school events.

### folder structure

`html files inside the /public folder are the college attendance.`

`html files inside the /shs-TimeIn folder are the shs attendance.`
